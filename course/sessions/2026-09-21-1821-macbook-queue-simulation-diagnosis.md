# 课程记录：队列模拟输入诊断

- 日期时间：2026-09-21 18:21
- 设备：MacBook
- 当前阶段：阶段 1
- 本次目标：诊断队列模拟器首版无输出的问题，保留学生原始作答。

## 学生作答与证据

- 独立用时：未记录。
- 最高提示等级：4（定位输入、循环范围和后续错误区域，修改仍由学生完成）。
- 完成情况：学生写出 `problems/007-queue-simulation/attempt.cpp`；C++03 编译成功，但给定样例预期三行 `3/6/9`，实际无输出。
- 相关文件：`problems/007-queue-simulation/attempt.cpp`。

## 诊断

- 已经做对：选择 `queue<int>`，使用 `push`、`front`、`pop` 和 `empty` 的方向正确。
- 第一处关键错误：用 `getchar()` 读取数字命令；它返回单个字符的编码，且不会跳过换行。最小输入 `1\n3\n` 的字节为 `49,10,51,10`，因此 `n` 实际得到 49，`switch` 也无法匹配 `case 3`。
- 错误类别：输入 / 实现 / 边界。
- 最小反例：`1\n3\n`，空队列查询应输出 `-1`，程序实际无输出。
- 后续待查：循环只执行 `n-1` 次；空队列查询未输出 `-1`；输出队列在按动态 `size()` 循环时同时 `pop`，可能提前结束。

## 掌握变化

- 新掌握：正在建立字符输入与整数解析的区别。
- 仍在学习：命令流解析、空队列分支和遍历时容器大小变化。
- 需要延迟复习：独立解释 `getchar()` 与 `cin >> int` 的差异。

## 下一步

1. 先将 `getchar()` 改成整数输入并把命令循环改为 `n` 次，保留原始尝试历史。
2. 重新运行最小反例和原样例，再逐一订正空队列查询与输出循环。
