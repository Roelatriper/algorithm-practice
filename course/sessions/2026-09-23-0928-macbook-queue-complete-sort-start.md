# 课程记录：队列模拟器结课与排序小节开始

- 日期时间：2026-09-23 09:28
- 设备：MacBook
- 当前阶段：阶段 1
- 本次目标：复验队列模拟器最终版本，并转入 `sort` 基础。

## 学生作答与证据

- 独立用时：未记录。
- 最高提示等级：4（此前已指出剩余错误位置和修正原则）。
- 完成情况：队列模拟器最终版本通过 C++03 严格编译和 5 组测试，覆盖普通操作、最后一条命令、空队列查询、连续查询和空队列出队。
- 相关文件：`problems/007-queue-simulation/attempt.cpp`。

## 诊断

- 已经做对：恰好处理 `n` 条命令；空队列查询输出 `-1`；先保存输出数量再逐项 `pop()`；每个查询结果独占一行。
- 第一处关键错误：本次最终版本未发现新的逻辑错误。
- 错误类别：此前为边界 / 实现，现已订正。
- 最小反例：`2\n1 3\n3\n`、`1\n3\n`、`3\n1 4\n3\n3\n` 均已通过。

## 掌握变化

- 新掌握：能用 `queue` 实现命令模拟，并正确处理空队列和循环边界；理解输出队列可作为程序层面的缓冲，使手工调试时输入输出分组。
- 仍在学习：`sort` 的调用范围和比较函数。
- 需要延迟复习：不借助提示重写一次队列模拟器，并解释为何循环内 `pop()` 时不能反复用变化中的 `size()` 作上界。

## 下一步

1. 复核 `sort(a.begin(), a.end())` 的排序范围。
2. 用 C++03 命名比较函数完成自定义排序。
