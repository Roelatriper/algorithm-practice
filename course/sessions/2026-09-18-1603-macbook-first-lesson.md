# 课程记录：第一课——工具链、测试与正确性

- 日期时间：2026-09-18 16:03
- 设备：MacBook
- 当前阶段：阶段 0
- 本次目标：完成 C++17 编译体检、区间调度测试与重写，并分析旧迷宫代码为何不是 BFS。

## 学生作答与证据

- 独立用时：未记录；中间间隔 7 天后继续。
- 最高提示等级：4。
- 完成情况：
  - 在 MacBook 上用统一脚本和 Apple Clang 成功运行 `dp.cpp`，输入 `5` 输出 `8`。
  - 能识别“能运行但答案错误”属于逻辑错误。
  - 能说明旧区间调度代码的排序索引映射，并识别 `n = 0` 错误输出 `1`。
  - 设计了首尾相接、普通重叠和击穿“开始时间最早”的反例。
  - `attempt.cpp` 使用 `struct`、`vector` 和 `sort`，订正后通过五组测试且无编译警告。
  - 能识别 `bfs.cpp` 在边界检查前递归、没有队列且不按层搜索。
- 相关文件：`attempt.cpp`、`区间调度贪心.cpp`、`bfs.cpp`。

## 诊断

- 已经做对：自定义排序比较函数正确按结束时间升序；能主动质疑并澄清 `1...n` 编号本身并非越界；反例设计有效。
- 第一处关键错误：贪心扫描默认选中 `data[0]` 却未将 `ans` 设为 1，且循环写成 `i <= n` 导致访问 `data[n]`。
- 错误类别：实现 / 边界 / 调试。
- 最小反例：`3; [1,5], [2,4], [4,5]`，期望 `2`，订正前输出 `1`；AddressSanitizer 同时确认 `data[n]` 堆越界。

## 掌握变化

- 新掌握：MacBook C++17 编译运行链路；`vector` 运行时长度与合法下标；区间调度的结束时间贪心与基本反例。
- 仍在学习：独立管理循环不变量与边界；BFS 的队列、访问标记、合法性判断和无权最短路分层性质。
- 需要延迟复习：不看现有代码重写区间调度，并独立解释 `ans = 1`、`i < n` 和 `tmp` 的循环不变量。

## 下一步

1. 在下次课开始时闭卷重写区间调度，并运行本次五组测试。
2. 重写通过后，进入 `vector` / `string` / `queue` / `sort` 与输入处理，再正式学习 BFS。
