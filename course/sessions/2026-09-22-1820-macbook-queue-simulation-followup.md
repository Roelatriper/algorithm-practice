# 课程记录：队列模拟器二次诊断

- 日期时间：2026-09-22 18:20
- 设备：MacBook
- 当前阶段：阶段 1
- 本次目标：复验修正输入后的队列模拟器，并定位剩余错答。

## 学生作答与证据

- 独立用时：未记录。
- 最高提示等级：4（定位错误位置和修正原则，未覆盖学生代码）。
- 完成情况：学生将 `getchar()` 改为 `cin >>`，输入解析问题已解决；原样例预期 `3/6/9`，当前只输出 `3 `。
- 相关文件：`problems/007-queue-simulation/attempt.cpp`。

## 诊断

- 已经做对：命令和参数已按整数读取；入队、出队与队首访问的基本使用正确。
- 第一处关键错误：循环条件仍为 `i < n-1`，导致最后一条命令不处理。最小反例 `2\n1 3\n3\n` 预期输出 `3`，实际无输出。
- 错误类别：边界 / 实现。
- 另有独立错误：输出队列在按当前 `size()` 控制循环时被 `pop()` 缩短，遗漏后续查询结果；空队列查询未输出 `-1`；查询结果未按每条一行输出。
- 最小反例：`3\n1 3\n3\n3\n` 当前只输出一次 `3 `，可与两次查询的预期区分。

## 掌握变化

- 新掌握：已将字符输入与整数解析区分并自行改正。
- 仍在学习：恰好处理 `n` 条命令、对会缩短的队列进行遍历、空队列查询。
- 需要延迟复习：解释为何 `for (i < output.size())` 与循环内 `pop()` 不能组合使用。

## 下一步

1. 将命令循环改为处理恰好 `n` 次。
2. 处理空队列查询，避免按动态 `size()` 遍历并同时 `pop()`；逐条输出一行。
3. 用 `2\n1 3\n3\n`、`1\n3\n` 和原样例复验。
