# 课程记录：P1427 stack 迁移

- 日期时间：2026-09-21 15:20
- 设备：MacBook
- 当前阶段：阶段 1
- 本次目标：将已完成的 `vector` 逆序输出迁移为 `stack` 后进先出实现。

## 学生作答与证据

- 独立用时：未记录。
- 最高提示等级：3（提供了 `stack` 基本 API，未提供完整程序）。
- 完成情况：学生独立写出 `stack-version.cpp`，用 C++03 编译无警告；`0`、`1 0`、`3 6 1 4 0` 和大整数样例均正确。
- 相关文件：`problems/1427-reverse-numbers/stack-version.cpp`。

## 诊断

- 已经做对：输入时 `push`，输出时先 `top` 再 `pop`；`empty()` 防止对空栈取顶。
- 第一处关键错误：本版在题目保证以 `0` 结尾的输入范围内未发现错误。
- 错误类别：无。
- 最小反例：空序列 `0` 已通过。

## 掌握变化

- 新掌握：能将逆序输出从 `vector` 倒序遍历迁移到 `stack` 后进先出；需后续延迟复习确认稳定性。
- 仍在学习：输入失败/EOF 的处理；`string` 与整行输入。
- 需要延迟复习：不看当前代码重写哨兵输入和栈操作。

## 下一步

1. 进入 `string` 与 `cin`、`getline` 的区别，先做简短前置诊断。
2. 后续练习中要求说明何时需要保存所有输入、何时可边读边处理。
