# 课程记录：P1093 奖学金多关键字排序

- 日期时间：2026-09-26 18:09
- 设备：MacBook
- 当前阶段：阶段 1
- 本次目标：独立组合结构体、`vector`、命名比较函数和 `sort`，完成一道多关键字排序题。

## 学生作答与证据

- 独立用时：40 多分钟。
- 最高提示等级：4（教师定位比较器、输出循环和越界区域，并明确关键字顺序；核心代码由学生自行订正）。
- 完成情况：学生报告已通过洛谷 P1093 官方 OJ；按学生要求不再本地运行。最终代码使用总分降序、语文分降序、学号升序的完整比较器，排序后直接输出前五名。
- 相关文件：`problems/P1093-scholarship/attempt.cpp`。

## 诊断

- 已经做对：能定义学生结构体、用 `vector<student>` 存储记录、计算总分、保存输入编号、把三层规则写入同一个严格比较函数，并将输出阶段简化为读取排序后的前五项。学生主动认识到不应在输出阶段堆叠大量分支补救排序。
- 第一处关键错误：初版比较器只比较总分，把并列处理留到输出循环；循环中同时修改 `i` 和 `fin`，造成跳项，并可能访问 `pupil[i+1]` 越界。订正版已把并列规则收回比较器。
- 错误类别：建模 / 实现 / 边界 / 调试。
- 最小反例：两名总分相同但语文分不同的学生必须由比较器决定顺序；初版在只有一组并列的输入上会跳过输出，并曾由 AddressSanitizer 检出越界。

## 掌握变化

- 新掌握：完成第一道结构体多关键字排序完整程序并获 OJ AC；能实现“总分降序—语文降序—学号升序”的顺位规则。
- 仍在学习：代码可读性与清理，包括字段命名、让只赋值的 `summing` 不返回无意义的 `bool`、移除未使用头文件和大段废弃注释；独立调试证据仍需增加。
- 需要延迟复习：7 天后闭卷重写比较器；用生日排序迁移“多层关键字 + 完全相等时的输入顺序”。

## 下一步

1. 完成洛谷 P1104《生日》作为迁移题，保存姓名、年月日和输入次序，继续使用 C++03 命名比较函数。
2. 至少自测年份不同、年月相同但日期不同、生日完全相同三种情况。
3. P1104 通过后再评估结构体多关键字排序是否达到阶段掌握标准。
