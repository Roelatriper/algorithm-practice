# 课程记录：sort EOF 输入与降序比较函数

- 日期时间：2026-09-23 17:36
- 设备：MacBook
- 当前阶段：阶段 1
- 本次目标：复查 `tmp.cpp` 的 EOF 读入、vector 扩容和 C++03 降序比较函数。

## 学生作答与证据

- 独立用时：未记录。
- 最高提示等级：4（此前已定位排序目标和范围问题；订正版由学生完成）。
- 完成情况：静态检查通过；没有编译或运行，边界测试待学生完成。
- 相关文件：`tmp.cpp`。

## 诊断

- 已经做对：使用空 `vector<int>` 配合 `while (cin >> tmp)` 与 `push_back` 读到 EOF；使用 C++03 命名比较函数 `down_sort`，并以 `a > b` 构成严格降序；把 `down_sort` 函数名传给 `sort`，对 `begin()` 到 `end()` 的全部已读元素排序。
- 第一处关键错误：订正版未发现静态逻辑错误。
- 错误类别：无新错误；验证未完成。
- 最小反例：尚未运行。建议覆盖空输入、单元素、重复值、负数和一般乱序输入。

## 掌握变化

- 新掌握：能独立组合 EOF 读取、`vector::push_back`、命名比较函数和 `sort` 全范围降序排序。
- 仍在学习：编译验证、边界测试，以及把排序范围迁移到子区间。
- 需要延迟复习：闭卷写出 C++03 比较函数并解释相等值时返回 `false` 的原因。

## 下一步

1. 用 Apple Clang 的 C++03 模式编译 `tmp.cpp`，并手动测试空输入、重复值和负数。
2. 练习只排序 vector 的一段区间，并继续阶段 1 的标准排序题。
