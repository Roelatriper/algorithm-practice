# 课程记录：sort 初次代码诊断

- 日期时间：2026-09-23 14:35
- 设备：MacBook
- 当前阶段：阶段 1
- 本次目标：检查 C++03 自定义降序比较函数练习。

## 学生作答与证据

- 独立用时：未记录。
- 最高提示等级：4（定位目标不匹配与排序范围错误，未代写订正代码）。
- 完成情况：`tmp.cpp` 调用了默认排序，但尚未实现指定的降序比较函数；当前排序范围包含未读入位置。
- 相关文件：`tmp.cpp`。

## 诊断

- 已经做对：包含 `<vector>`、`<algorithm>`；使用 `cin >>` 循环读取，并记录已读数量 `i`；知道通过 `sort` 排序。
- 第一处关键错误：`sort(a.begin(), a.end())` 对固定长度 10000 的整个 vector 做默认升序，而练习要求 C++03 命名比较函数实现降序。
- 错误类别：实现 / 边界。
- 最小反例：输入 `4 1` 时，剩余元素仍为初始化的 0；对整个 vector 升序排序后，输出前两个元素会是 `0 0`，而非目标降序结果 `4 1`。
- 风险：容量到达 10000 后，下一轮循环会先访问 `a[10000]` 再检查提取是否成功，越界；本次未运行代码。

## 掌握变化

- 新掌握：能在一段完整程序中组织输入、`vector` 和 `sort`。
- 仍在学习：比较函数的定义、函数名传参、严格比较，以及只排序已读区间。
- 需要延迟复习：自定义比较函数实现降序；EOF 读取与固定容量保护。

## 下一步

1. 在 `tmp.cpp` 中补上 C++03 命名降序比较函数，并传给 `sort`。
2. 只对实际读入的元素排序，再用重复元素确认比较函数相等时返回 `false`。
