# 课程记录：P5015 字符串输入

- 日期时间：2026-09-21 17:11
- 设备：MacBook
- 当前阶段：阶段 1
- 本次目标：用 `getline` 读取含空格的标题并逐字符统计。

## 学生作答与证据

- 独立用时：未记录。
- 最高提示等级：2（教师提示 `string` 下标访问和空格字符写法）。
- 完成情况：学生写出 `problems/P5015-title-count/attempt.cpp`，使用 `getline` 读取整行，跳过空格计数。C++03 编译后，纯数字、混合空格、全空格、单字符与空行等六组测试均正确。
- 相关文件：`problems/P5015-title-count/attempt.cpp`。

## 诊断

- 已经做对：能够保留字符串中的空格，并用下标逐字符检查。
- 第一处关键错误：算法结果无误；循环中 `int i < a.size()` 混用有符号与无符号整数，产生编译警告。
- 错误类别：实现规范。
- 最小反例：无错答反例；空行输入正确输出 `0`。
- 后续改进：显式包含 `<string>`，并用 C++03 兼容写法统一循环下标与长度的类型。

## 掌握变化

- 新掌握：`getline` 与 `cin >> string` 在空格处理上的区别，`string` 可按下标访问字符。
- 仍在学习：混合 `cin >>` 与 `getline` 时处理残留换行，以及编译警告的清理。
- 需要延迟复习：独立完成一题混合数字与整行字符串输入的小题。

## 下一步

1. 清理 P5015 的 `<string>` 头文件和符号类型比较警告，不覆盖真实作答历史。
2. 做一题同时包含数字输入与整行字符串输入的轻微变式。
