# 课程记录：结构体与多关键字排序起步

- 日期时间：2026-09-26 16:23
- 设备：MacBook
- 当前阶段：阶段 1
- 本次目标：按学生请求暂缓指针细节，转入结构体与多关键字自定义排序。

## 学生作答与证据

- 独立用时：未记录。
- 最高提示等级：5（学生口述判断框架后，教师给出完整命名比较函数作为参考；尚未独立实现完整程序）。
- 完成情况：能按“得分降序、罚时升序、编号升序”正确排出样例顺序 `2, 4, 1, 3`，并能口述先比较 `score`、相等再比较 `penalty`、最后比较 `number`。了解本课暂用命名比较函数而不引入 `operator<`，并确认 C++ 中定义具名 `struct` 不需要 `typedef`。
- 相关文件：无；本次为口述建模与代码阅读。

## 诊断

- 已经做对：理解后一个关键字只有在前一个关键字相等时才参与；样例排序结果正确；主动提出 `operator` 与 `typedef` 的关系。
- 第一处关键错误：口述实现时把编号升序写成 `a.number > b.number`，并认为三个字段全相等应报错；正确要求是编号使用 `<`，全部相等返回 `false`。
- 错误类别：实现 / 排序规则。
- 最小反例：两条完全相同的记录要求 `better(x, x) == false`；`a=(90,1)`、`b=(100,100)` 可击穿“不检查前一关键字是否相等就继续看罚时”的写法。

## 掌握变化

- 新掌握：能理解结构体记录的多关键字排序顺序，并正确手排一个样例。
- 仍在学习：独立写出严格比较函数；使用 `vector<Player>`、成员访问和 `sort` 组成完整 C++03 程序。
- 需要延迟复习：比较器全部关键字相等时返回 `false`；编号升序使用 `<`。

## 下一步

1. 独立实现并测试一个 `Player` 多关键字排序完整程序，至少覆盖普通情况、第一关键字相等、前两关键字相等和完全重复记录。
2. 通过后进入结构体排序迁移题；指针与 `int*&` 按学生请求暂缓，后续结合实际场景再回填，不阻塞当前主线。
