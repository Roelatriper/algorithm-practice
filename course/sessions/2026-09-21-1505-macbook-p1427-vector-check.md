# 课程记录：P1427 vector 版验证

- 日期时间：2026-09-21 15:05
- 设备：MacBook
- 当前阶段：阶段 1
- 本次目标：验证 P1427 倒序循环的空容器处理，并进入 `stack` 迁移。

## 学生作答与证据

- 独立用时：未记录。
- 最高提示等级：3（教师给出倒序循环写法）。
- 完成情况：学生将逆序循环改为从 `s.size()` 倒数、访问 `s[i - 1]`。用 C++03 编译，输入 `0`、`1 0`、`3 6 1 4 0`、`2147483647 2 0` 均正常退出且逆序结果正确。
- 相关文件：`problems/1427-reverse-numbers/attempt.cpp`。

## 诊断

- 已经做对：正确保存哨兵前的数据，并在倒序访问时避开空容器的 `size()-1` 下溢。
- 第一处关键错误：前一版附加条件会漏掉只有一个元素的情况；本版已修复。
- 错误类别：边界。
- 最小反例：`1 0`，前一版无输出，本版输出 `1`。
- 剩余小问题：`sum` 未使用，C++03 编译产生警告。

## 掌握变化

- 新掌握：P1427 的 `vector` 存储与安全倒序遍历，已通过即时测试。
- 仍在学习：`stack` 的后进先出操作，以及无提示迁移实现。
- 需要延迟复习：关闭参考代码后重新实现哨兵输入与逆序输出。

## 下一步

1. 保留 `attempt.cpp`，新建 `stack-version.cpp`，用 C++03 的 `stack<int>` 独立完成同题。
2. 测试 `0`、`1 0`、`3 6 1 4 0`，并说明栈版与 vector 版在访问方式上的区别。
