# 当前课程状态

> 最后更新：2026-09-23（MacBook）
>
> 本文件是跨设备、跨聊天的当前状态。详细历史见 `sessions/`。

## 当前定位

- 阶段：1——C++ 竞赛基本功。
- 当前课：阶段 1 的 `sort` 基础与命名比较函数；队列命令模拟已通过。`tmp.cpp` 已改为 EOF + `vector::push_back`，并用 C++03 命名比较函数降序排序；静态检查通过，编译和边界测试待完成。默认只用 C++03 兼容语法。
- 默认近期目标：蓝桥杯或程序设计校赛入门。
- 长期目标：达到 ICPC/CCPC 校队选拔的基础水平。
- 尚待学生确认：第一目标赛事；学期中每周可稳定投入时间。

## 已有证据

| 主题 | 状态 | 最近证据 |
|---|---|---|
| C++ 基础语法 | 入门后期 | 能完成简单数组、循环、函数和递归程序 |
| STL | 学习中 | P1427 `vector` 与 `stack` 双版本通过；P5015 主体通过；队列模拟通过 C++03 严格编译及 5 组边界测试 |
| DFS/回溯 | 概念入门 | 子集代码为课后整理，未独立完成 |
| BFS | 未掌握 | 能识别旧代码无队列、无访问标记且边界检查过晚；尚不会实现 |
| 贪心 | 初步 | 区间调度延迟重写通过七组测试，状态跟踪正确；P1909 原始实现通过 300 组对拍 |
| 基础 DP | 初步 | 走楼梯记忆化递归可完成，尚无迁移证据 |
| 树 | 未掌握 | 输入、建树、初始化和递归边界均存在障碍 |
| 调试 | 入门 | 已用反例复现错答，并用 AddressSanitizer 确认 `data[n]` 越界；仍需独立运用 |

## 当前任务

1. 复核 `sort(a.begin(), a.end())` 的排序范围与默认升序行为。
2. 编译并测试 `tmp.cpp`（空输入、单元素、重复值、负数），然后练习只排序指定子区间；避免 lambda、`auto` 等 C++11 语法。
3. 延迟复习队列模拟器；P5015 类型比较警告、P1427 未使用变量警告及 P1909 精简订正版均待后续清理，不阻塞当前课。

## 第一课过关标准

- 能在 MacBook 上通过统一脚本编译并运行一个 C++17 文件。
- 能说清编译错误、运行错误、逻辑错误和复杂度问题的区别。
- 能为区间调度设计最小、普通、边界和反例测试。
- 能使用 `vector` 与 `sort` 重写区间调度，并处理 `n = 0`。
- 能解释当前迷宫代码为什么不是 BFS。

## 最近一次交接

- 记录：`sessions/2026-09-23-1736-macbook-sort-review.md`
- 下一位教师应让学生编译并测试 `tmp.cpp`，再练习 vector 子区间排序；队列模拟器已经结课。
