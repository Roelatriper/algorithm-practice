# 当前课程状态

> 最后更新：2026-09-26（MacBook）
>
> 本文件是跨设备、跨聊天的当前状态。详细历史见 `sessions/`。

## 当前定位

- 阶段：1——C++ 竞赛基本功。
- 当前课：正在进行“指针、引用与参数传递”两段课。第一段已建立值传递、普通引用、取地址和解引用的初步模型；能写数值交换，但未初始化指针和 `int*&` 的两层映射尚未掌握。下次课按学生要求再做延迟检查，然后讲空指针、野指针与数组传参长度丢失；通过后进入结构体与多关键字排序。默认只用 C++03 兼容语法。
- 第一硬目标：预计 2027 年 3 月左右的校队选拔网赛，确切日期待寒假公告；蓝桥杯或程序设计校赛作为后续训练与检验。
- 长期目标：达到 ICPC/CCPC 校队选拔的基础水平。
- 进度约束：距预计选拔约 5～6 个月，课程改用压缩路线；目标在 2027 年 1 月底前完成核心知识覆盖，2 月进入完整 4 小时模拟与补弱。
- 已确认赛制：预计为网赛；往届使用牛客 ACM 模式、4 小时、可选 Clang++ 18，9 道题面已整理到 `problems/team-selection/`。2027 年负责人变更，因此旧赛制细节和题型均只作参考，往届题仅作诊断和模拟。
- 选拔策略：按择优录取的相对排名准备，目标是稳定取得基础分并提高中档题命中率；不以覆盖全部高级专题为目标。
- 时间预算：常态每周 12 小时；党务或考试繁忙周保底 8 小时，短期极忙可用 5～6 小时维持；2027 年 2 月全长模拟阶段目标约 14 小时。连续两周低于 8 小时时应主动调整范围。
- 外部事务风险：党务中心申请若通过可能占用训练时间；在结果和真实占用明确前不预设小时数，发生后按优先级重排。
- 尚待寒假公告确认：确切日期与平台、题数与罚时/计分规则、语言标准、资料与设备规则、新负责人公布的范围。

## 已有证据

| 主题 | 状态 | 最近证据 |
|---|---|---|
| C++ 基础语法 | 入门后期 | 能完成简单数组、循环、函数和递归程序 |
| STL | 学习中 | 能完成整数 vector 的全范围及一基闭区间降序排序；2026-09-26 延迟回忆中比较方向写反，且未能独立解释严格比较，尚需再复查 |
| 指针、引用与传参 | 学习中 | 能区分值传递、普通引用和通过指针修改对象，并写对数值交换；两次解引用未初始化指针，`int*&` 地址交换需延迟验证 |
| DFS/回溯 | 概念入门 | 子集代码为课后整理，未独立完成 |
| BFS | 未掌握 | 能识别旧代码无队列、无访问标记且边界检查过晚；尚不会实现 |
| 贪心 | 初步 | 区间调度延迟重写通过七组测试，状态跟踪正确；P1909 原始实现通过 300 组对拍 |
| 基础 DP | 初步 | 走楼梯记忆化递归可完成，尚无迁移证据 |
| 树 | 未掌握 | 输入、建树、初始化和递归边界均存在障碍 |
| 调试 | 入门 | 已见编译警告与 AddressSanitizer 分别定位未初始化指针和非法解引用；本次诊断由教师完成，仍需独立运用 |

## 当前任务

1. 再次延迟闭卷复查 C++03 命名比较函数的方向和严格比较规则；子区间端点转换已有当堂证据。
2. 完成指针与传参第二段：先复查“合法指向—解引用—数值交换—地址交换”，再讲空指针、野指针和数组传参长度丢失；不扩展二级指针、函数指针或动态内存。
3. 学习结构体与多关键字自定义排序，继续避免 lambda、`auto` 等 C++11 语法。
4. 完成上述两项语言基础后，用 `快速幂`、`最小生成树`、`坂田银时吃多了` 做 120 分钟冷启动诊断；不以 AC 数作为唯一评价。
5. 从 2026 年 10 月起每周安排 90～120 分钟短赛；1 月底首次全长诊断，2 月每周全长模拟。短赛题兼作诊断或迁移，不额外增加 12 小时预算。
6. 并行安排两次 Git 实践课，使用临时本地远程仓库演练四层模型、分支、同步和冲突，不污染课程仓库。

## 第一课过关标准

- 能在 MacBook 上通过统一脚本编译并运行一个 C++17 文件。
- 能说清编译错误、运行错误、逻辑错误和复杂度问题的区别。
- 能为区间调度设计最小、普通、边界和反例测试。
- 能使用 `vector` 与 `sort` 重写区间调度，并处理 `n = 0`。
- 能解释当前迷宫代码为什么不是 BFS。

## 最近一次交接

- 记录：`sessions/2026-09-26-1609-macbook-pointers-parameters-part1.md`
- 下一位教师应遵守学生的暂停请求：本次不再抽查 `int*&`。下次先用短例子延迟检查指针必须先建立合法指向、`int tmp = *x` 与 `int* tmp = x` 的区别，以及数值交换和地址交换；随后讲空指针、野指针与数组传参长度丢失。完成独立验证后进入结构体多关键字排序。
