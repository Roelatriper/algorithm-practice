# 当前课程状态

> 最后更新：2026-09-21（MacBook）
>
> 本文件是跨设备、跨聊天的当前状态。详细历史见 `sessions/`。

## 当前定位

- 阶段：1——C++ 竞赛基本功。
- 当前课：阶段 1 的 `string` 与输入处理；P1427 的 `vector` 和 `stack` 两版均已通过。默认只用 C++03 兼容语法。
- 默认近期目标：蓝桥杯或程序设计校赛入门。
- 长期目标：达到 ICPC/CCPC 校队选拔的基础水平。
- 尚待学生确认：第一目标赛事；学期中每周可稳定投入时间。

## 已有证据

| 主题 | 状态 | 最近证据 |
|---|---|---|
| C++ 基础语法 | 入门后期 | 能完成简单数组、循环、函数和递归程序 |
| STL | 学习中 | 区间调度闭卷重写通过七组测试，但 `sort` 调用需 3 级提示；P1427 `vector` 版在提示后通过边界测试，`stack` 迁移版独立实现并通过四组测试 |
| DFS/回溯 | 概念入门 | 子集代码为课后整理，未独立完成 |
| BFS | 未掌握 | 能识别旧代码无队列、无访问标记且边界检查过晚；尚不会实现 |
| 贪心 | 初步 | 区间调度延迟重写通过七组测试，状态跟踪正确；P1909 原始实现通过 300 组对拍 |
| 基础 DP | 初步 | 走楼梯记忆化递归可完成，尚无迁移证据 |
| 树 | 未掌握 | 输入、建树、初始化和递归边界均存在障碍 |
| 调试 | 入门 | 已用反例复现错答，并用 AddressSanitizer 确认 `data[n]` 越界；仍需独立运用 |

## 当前任务

1. 开始 `string` 输入基础：先区分 `cin >> s` 与 `getline(cin, s)` 的读取边界。
2. 后续做一题包含空格的字符串练习，并继续只用 C++03 兼容语法。
3. P1427 `vector` 版仍有未使用的 `sum` 警告；P1909 边读边处理订正版尚未复验，两者不阻塞新课。

## 第一课过关标准

- 能在 MacBook 上通过统一脚本编译并运行一个 C++17 文件。
- 能说清编译错误、运行错误、逻辑错误和复杂度问题的区别。
- 能为区间调度设计最小、普通、边界和反例测试。
- 能使用 `vector` 与 `sort` 重写区间调度，并处理 `n = 0`。
- 能解释当前迷宫代码为什么不是 BFS。

## 最近一次交接

- 记录：`sessions/2026-09-21-1520-macbook-p1427-stack-transfer.md`
- 下一位教师应从 `string` 与输入处理继续，P1427 仅在延迟复习时重做。
