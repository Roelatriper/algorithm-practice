# 当前课程状态

> 最后更新：2026-09-18（MacBook）
>
> 本文件是跨设备、跨聊天的当前状态。详细历史见 `sessions/`。

## 当前定位

- 阶段：0——编码体检、工具链与正确性。
- 当前课：`lessons/01-cpp-toolchain-and-correctness.md`（课堂内容完成，待延迟重写验证）。
- 默认近期目标：蓝桥杯或程序设计校赛入门。
- 长期目标：达到 ICPC/CCPC 校队选拔的基础水平。
- 尚待学生确认：第一目标赛事；学期中每周可稳定投入时间。

## 已有证据

| 主题 | 状态 | 最近证据 |
|---|---|---|
| C++ 基础语法 | 入门后期 | 能完成简单数组、循环、函数和递归程序 |
| STL | 学习中 | 能在提示后用 `vector` 与自定义比较函数调用 `sort` |
| DFS/回溯 | 概念入门 | 子集代码为课后整理，未独立完成 |
| BFS | 未掌握 | 能识别旧代码无队列、无访问标记且边界检查过晚；尚不会实现 |
| 贪心 | 初步 | 区间调度 `vector + sort` 版经 4 级提示订正后通过五组测试，待闭卷重写 |
| 基础 DP | 初步 | 走楼梯记忆化递归可完成，尚无迁移证据 |
| 树 | 未掌握 | 输入、建树、初始化和递归边界均存在障碍 |
| 调试 | 入门 | 已用反例复现错答，并用 AddressSanitizer 确认 `data[n]` 越界；仍需独立运用 |

## 当前任务

1. 下次课先不看 `attempt.cpp`，闭卷重写区间调度并通过已设计的五组测试。
2. 能独立解释 `ans = 1`、`i < n` 与 `tmp` 在扫描中的含义。
3. 延迟重写通过后进入阶段 1，学习 `vector` / `string` / `queue` / `sort` 与输入处理。

## 第一课过关标准

- 能在 MacBook 上通过统一脚本编译并运行一个 C++17 文件。
- 能说清编译错误、运行错误、逻辑错误和复杂度问题的区别。
- 能为区间调度设计最小、普通、边界和反例测试。
- 能使用 `vector` 与 `sort` 重写区间调度，并处理 `n = 0`。
- 能解释当前迷宫代码为什么不是 BFS。

## 最近一次交接

- 记录：`sessions/2026-09-18-1603-macbook-first-lesson.md`
- 下一位教师应先做区间调度延迟闭卷重写，通过后再进入阶段 1。
