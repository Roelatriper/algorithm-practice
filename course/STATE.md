# 当前课程状态

> 最后更新：2026-09-24（MacBook）
>
> 本文件是跨设备、跨聊天的当前状态。详细历史见 `sessions/`。

## 当前定位

- 阶段：1——C++ 竞赛基本功。
- 当前课：阶段 1 的 `sort` 基础与子区间排序已完成当堂验证；下一课进入结构体与多关键字自定义排序。`tmp.cpp` 已通过 C++03 严格编译及空输入、单元素、重复值、负数测试；子区间作答也通过严格编译和边界测试。默认只用 C++03 兼容语法。
- 第一硬目标：2027 年 4 月校队选拔；蓝桥杯或程序设计校赛作为同期训练与检验。
- 长期目标：达到 ICPC/CCPC 校队选拔的基础水平。
- 进度约束：距校队选拔约 6 个多月，课程改用压缩路线，优先覆盖选拔高频核心模型并尽早加入限时训练。
- 已确认赛制：牛客 ACM 模式，4 小时，可选 Clang++ 18；往届 9 道题面已整理到 `problems/team-selection/`。2027 年负责人变更，因此往届题仅作诊断和模拟，不用于押题。
- 尚待学生确认：选拔的确切日期、题数与罚时/计分规则、语言标准选项、是否允许纸质资料、新负责人公布的范围；学期中每周可稳定投入时间。

## 已有证据

| 主题 | 状态 | 最近证据 |
|---|---|---|
| C++ 基础语法 | 入门后期 | 能完成简单数组、循环、函数和递归程序 |
| STL | 学习中 | P1427 `vector` 与 `stack` 双版本通过；队列模拟通过；能独立用 C++03 命名比较函数完成整数 vector 的全范围及一基闭区间降序排序 |
| DFS/回溯 | 概念入门 | 子集代码为课后整理，未独立完成 |
| BFS | 未掌握 | 能识别旧代码无队列、无访问标记且边界检查过晚；尚不会实现 |
| 贪心 | 初步 | 区间调度延迟重写通过七组测试，状态跟踪正确；P1909 原始实现通过 300 组对拍 |
| 基础 DP | 初步 | 走楼梯记忆化递归可完成，尚无迁移证据 |
| 树 | 未掌握 | 输入、建树、初始化和递归边界均存在障碍 |
| 调试 | 入门 | 已用反例复现错答，并用 AddressSanitizer 确认 `data[n]` 越界；仍需独立运用 |

## 当前任务

1. 延迟闭卷重写 C++03 命名比较函数和一基闭区间排序，并解释相等值时必须返回 `false`。
2. 学习结构体与多关键字自定义排序，继续避免 lambda、`auto` 等 C++11 语法。
3. 根据 4 小时 ACM 赛制安排从短赛到全长赛的递进训练；待剩余规则和每周投入确认后，定稿 2026 年 10 月至 2027 年 4 月周计划。

## 第一课过关标准

- 能在 MacBook 上通过统一脚本编译并运行一个 C++17 文件。
- 能说清编译错误、运行错误、逻辑错误和复杂度问题的区别。
- 能为区间调度设计最小、普通、边界和反例测试。
- 能使用 `vector` 与 `sort` 重写区间调度，并处理 `n = 0`。
- 能解释当前迷宫代码为什么不是 BFS。

## 最近一次交接

- 记录：`sessions/2026-09-24-1132-macbook-team-selection-format.md`
- 下一位教师应先做一次命名比较函数与子区间排序的闭卷回忆，再进入结构体和多关键字排序；后续课程需要加入 ACM 短赛并逐步延长到 4 小时。往届题只作诊断和模拟。
