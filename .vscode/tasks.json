{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build current C++ file",
            "type": "shell",
            "command": "${workspaceFolder}/scripts/build.sh",
            "args": [
                "${file}"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$gcc"
            ]
        }
    ]
}
